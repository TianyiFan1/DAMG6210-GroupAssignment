"""Utility modules for CoHabitant Streamlit app.

Modules:
    db              - Database connection pool, query execution, tenant helpers
    auth            - auth_gate() middleware for page-level RBAC
    state           - AppState class for centralized session management
    financial_logic - Pure financial split math (no side effects)
"""
